$(function() {
	$('.header img').on('mouseenter touchstart', function() {
		$(this).addClass('flip animated infinite');
	});
	$('.header img').on('mouseleave touchend', function() {
		$(this).removeClass('flip animated infinite');
	});
});
