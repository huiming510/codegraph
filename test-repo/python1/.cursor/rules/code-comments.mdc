---
description: 代码注释规则（后端 + 前端），便于业务 check 与可维护性，防止垃圾代码堆叠
globs: src/backend/**/*,src/frontend/**/*
alwaysApply: false
---

# 代码注释规则

本规则适用于 `src/backend` 与 `src/frontend` 下所有业务代码。**对外接口与复杂逻辑必须有清晰注释**，便于业务对照、Review 与后续维护，避免无注释或无效注释导致的“垃圾代码”堆叠。

---

## 1. 必须注释的范围

| 对象 | 要求 | 后端（Python） | 前端（JS/Vue） |
|------|------|----------------|----------------|
| **文件/模块** | 简要说明本文件职责、所属业务模块 | 文件顶部 docstring 或 `#` 块注释 | 文件顶部 `/** ... */` 或 `// 模块说明` |
| **对外 API（路由/接口）** | 说明接口用途、主要参数、返回值含义 | 路由函数 docstring（用途、参数、返回） | JSDoc 或函数上方注释（用途、参数、返回） |
| **导出的公共方法** | 说明做什么、关键参数 | 函数 docstring | JSDoc 或简短注释 |
| **复杂业务逻辑** | 说明“做什么”和“为什么”（算法、分支、边界） | 段落前 `#` 或行内注释 | `//` 块注释 |
| **非显而易见的分支/常量** | 说明业务含义或选择理由 | 行上/行尾注释 | 同上 |

**“对外”定义**：后端指对前端或其它服务暴露的 HTTP 路由；前端指从 `api/modules` 导出、被多个页面/组件调用的方法，以及路由、Store 中与业务强相关的方法。

---

## 2. 注释内容要求

- **做什么**：必写。一句话说明该模块/函数/逻辑块的作用。
- **为什么**：在“非显而易见”时写（如特殊业务规则、兼容历史、性能取舍）。
- **参数与返回值**：对外 API 与公共方法必须写；后端用 docstring 描述，前端用 JSDoc 的 `@param`、`@returns`（或等价说明）。

注释应与代码一致；修改代码时同步修改注释，**禁止保留过时或与实现不符的注释**。

---

## 3. 格式约定

### 3.1 后端（Python）

- **模块/文件头**：三引号 docstring 或 `# ---- 模块说明 ----` 块，说明职责与所属业务（如知识库、文档解析）。
- **路由/公共函数**：使用 **docstring**（Google 风格推荐），包含用途、Args、Returns；若为简单 CRUD，可一行概括。

```python
def get_knowledge_base(db: AsyncSession, kb_id: int) -> KnowledgeBase | None:
    """根据 ID 获取知识库；不存在返回 None。"""
    ...

@router.get("/{kb_id}")
async def get_kb(kb_id: int, db: AsyncSession = Depends(get_db)):
    """
    获取单个知识库详情。
    用于前端详情页与下拉选择；未找到返回 404。
    """
    ...
```

- **复杂逻辑**：段落前用 `# 说明业务或算法意图`，避免只写“这里循环”等无信息量注释。

### 3.2 前端（JS/Vue）

- **模块/文件头**：`/** 本模块：xxx 相关 API / 本页面：xxx 功能 */` 或等效注释。
- **api/modules 导出方法**：每个导出方法应有注释或 JSDoc，说明用途、参数、返回值（与后端接口对应时可写“对应后端 GET /api/xxx”）。

```javascript
/**
 * 获取知识库列表，支持分页与筛选
 * @param {Object} params - { skip, limit, keyword }
 * @returns {Promise<{ items, total }>} 对应后端 success_response 的 data
 */
export const getKnowledgeBases = (params = {}) => request.get("/knowledge-bases", { params });
```

- **复杂逻辑/分支**：用 `// 说明业务含义或选择理由`，避免无信息量注释。

---

## 4. 禁止项（防止垃圾代码）

- **禁止**大段注释掉的废代码：不再使用的代码应**删除**，不要长期保留注释块；若确需保留须注明原因与日期。
- **禁止**无信息量注释：如 `# 循环`、`// 判断`、`# 这里很复杂` 等不说明业务或意图的注释；要么写清“做什么/为什么”，要么不写。
- **禁止**注释与实现不一致：发现不一致时以代码为准并**立即修正注释**。

---

## 5. 与业务 check 的关系

- 业务实现检查（需求与实现对照）时，可依据**对外 API 与关键业务逻辑是否有清晰注释**判断可维护性与是否“堆叠垃圾代码”。
- **建议**：对外接口（后端路由、前端 api/modules 导出）无用途说明或参数/返回值说明的，在 Review 中标注为「需补充注释」；复杂逻辑无注释的，标注为「建议补充意图说明」。
